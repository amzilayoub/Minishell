#include "minishell.h"
/*
char	*treat_deci(char *line)
{
	int i;
}

void	backslash_parser(char *line, int index)
{
	char	*pre;
	char	*suff;

	pre = ft_substr(line, 0, index);
	if (line[index + 1] == 'x')
		suff = treat_hexa(line);
	else
		suff = treat_deci(line);
}
*/
